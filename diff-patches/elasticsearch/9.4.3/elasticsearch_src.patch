diff --git a/build-conventions/build.gradle b/build-conventions/build.gradle
index 2a98df5..cf1fa77 100644
--- a/build-conventions/build.gradle
+++ b/build-conventions/build.gradle
@@ -12,6 +12,15 @@ import org.gradle.plugins.ide.eclipse.model.SourceFolder
 
 buildscript {
   repositories {
+    maven {
+      url "https://maven.loongnix.cn/loongarch/maven/"
+      content {
+        includeModule "net.java.dev.jna", "jna"
+        includeModule "net.java.dev.jna", "jna-platform"
+        includeModule "org.lz4", "lz4-java"
+        includeModule "com.google.protobuf", "protoc"
+      }
+    }
     mavenCentral()
   }
 }
@@ -63,6 +72,15 @@ gradlePlugin {
 }
 
 repositories {
+    maven {
+      url "https://maven.loongnix.cn/loongarch/maven/"
+      content {
+        includeModule "net.java.dev.jna", "jna"
+        includeModule "net.java.dev.jna", "jna-platform"
+        includeModule "org.lz4", "lz4-java"
+        includeModule "com.google.protobuf", "protoc"
+      }
+    }
   mavenCentral()
   gradlePluginPortal()
 }
diff --git a/build-tools-internal/build.gradle b/build-tools-internal/build.gradle
index da4fb45..886dd34 100644
--- a/build-tools-internal/build.gradle
+++ b/build-tools-internal/build.gradle
@@ -277,6 +277,15 @@ tasks.named('licenseHeaders').configure {
  *****************************************************************************/
 
 repositories {
+    maven {
+      url "https://maven.loongnix.cn/loongarch/maven/"
+      content {
+        includeModule "net.java.dev.jna", "jna"
+        includeModule "net.java.dev.jna", "jna-platform"
+        includeModule "org.lz4", "lz4-java"
+        includeModule "com.google.protobuf", "protoc"
+      }
+    }
   mavenCentral()
   gradlePluginPortal()
 }
diff --git a/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/ElasticsearchJavaBasePlugin.java b/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/ElasticsearchJavaBasePlugin.java
index 4e05076..7f69e5d 100644
--- a/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/ElasticsearchJavaBasePlugin.java
+++ b/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/ElasticsearchJavaBasePlugin.java
@@ -108,7 +108,7 @@ public class ElasticsearchJavaBasePlugin implements Plugin<Project> {
             // fail on all javac warnings.
             // TODO Discuss moving compileOptions.getCompilerArgs() to use provider api with Gradle team.
             List<String> compilerArgs = compileOptions.getCompilerArgs();
-            compilerArgs.add("-Werror");
+            
             // Prepend -Xlint:none to suppress the "using incubating module(s)" mandatory warning
             // that javac emits when jdk.incubator.vector is used. On JDK 21 this warning has no
             // named lint category and cannot be individually suppressed. -Xlint:none clears all
@@ -124,7 +124,7 @@ public class ElasticsearchJavaBasePlugin implements Plugin<Project> {
                 xlintExclusions += ",-incubating";
             }
             compilerArgs.add("-Xlint:" + xlintExclusions);
-            compilerArgs.add("-Xdoclint:all");
+            compilerArgs.add("-Xdoclint:none");
             compilerArgs.add("-Xdoclint:-missing");
             compileOptions.setEncoding("UTF-8");
             compileOptions.setIncremental(true);
diff --git a/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/Jdk.java b/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/Jdk.java
index dfbfe0f..ddd4770 100644
--- a/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/Jdk.java
+++ b/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/Jdk.java
@@ -25,7 +25,7 @@ import java.util.regex.Pattern;
 
 public class Jdk implements Buildable, Iterable<File> {
 
-    private static final List<String> ALLOWED_ARCHITECTURES = List.of("aarch64", "x64");
+    private static final List<String> ALLOWED_ARCHITECTURES = List.of("aarch64", "x64", "loongarch64");
     private static final List<String> ALLOWED_VENDORS = List.of("adoptium", "openjdk", "zulu");
     private static final List<String> ALLOWED_PLATFORMS = List.of("darwin", "linux", "windows", "mac");
     private static final Pattern VERSION_PATTERN = Pattern.compile(
diff --git a/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/MrjarPlugin.java b/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/MrjarPlugin.java
index ed5e4c9..1658eaa 100644
--- a/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/MrjarPlugin.java
+++ b/build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/MrjarPlugin.java
@@ -118,6 +118,12 @@ public class MrjarPlugin implements Plugin<Project> {
         });
     }
 
+    private static int toolchainVersionFor(int javaVersion) {
+        return javaVersion == 22 ? 23 : javaVersion;
+    }
+    private static int compileReleaseVersionFor(int javaVersion) {
+        return javaVersion == 22 ? 23 : javaVersion;
+    }
     private SourceSet addSourceSet(
         Project project,
         JavaPluginExtension javaExtension,
@@ -133,15 +139,15 @@ public class MrjarPlugin implements Plugin<Project> {
 
         project.getTasks().withType(JavaCompile.class).named(sourceSet.getCompileJavaTaskName()).configure(compileTask -> {
             compileTask.getJavaCompiler()
-                .set(javaToolchains.compilerFor(spec -> { spec.getLanguageVersion().set(JavaLanguageVersion.of(javaVersion)); }));
-            compileTask.setSourceCompatibility(Integer.toString(javaVersion));
+                .set(javaToolchains.compilerFor(spec -> { spec.getLanguageVersion().set(JavaLanguageVersion.of(toolchainVersionFor(javaVersion))); }));
+            compileTask.setSourceCompatibility(Integer.toString(compileReleaseVersionFor(javaVersion)));
             CompileOptions compileOptions = compileTask.getOptions();
-            compileOptions.getRelease().set(javaVersion);
+            compileOptions.getRelease().set(compileReleaseVersionFor(javaVersion));
         });
         if (isMainSourceSet) {
             project.getTasks().register(sourceSet.getJavadocTaskName(), Javadoc.class, javadocTask -> {
                 javadocTask.getJavadocTool().set(javaToolchains.javadocToolFor(spec -> {
-                    spec.getLanguageVersion().set(JavaLanguageVersion.of(javaVersion));
+                    spec.getLanguageVersion().set(JavaLanguageVersion.of(toolchainVersionFor(javaVersion)));
                 }));
             });
         }
@@ -179,7 +185,7 @@ public class MrjarPlugin implements Plugin<Project> {
         project.getTasks().withType(Javadoc.class).named(name -> name.equals(sourceSet.getJavadocTaskName())).configureEach(javadocTask -> {
             CoreJavadocOptions options = (CoreJavadocOptions) javadocTask.getOptions();
             options.addBooleanOption("-enable-preview", true);
-            options.addStringOption("-release", String.valueOf(javaVersion));
+            options.addStringOption("-release", String.valueOf(compileReleaseVersionFor(javaVersion)));
         });
     }
 
@@ -218,7 +224,7 @@ public class MrjarPlugin implements Plugin<Project> {
                 });
             } else {
                 testTask.getJavaLauncher()
-                    .set(javaToolchains.launcherFor(spec -> spec.getLanguageVersion().set(JavaLanguageVersion.of(javaVersion))));
+                    .set(javaToolchains.launcherFor(spec -> spec.getLanguageVersion().set(JavaLanguageVersion.of(toolchainVersionFor(javaVersion)))));
             }
         });
 
diff --git a/build-tools-internal/version.properties b/build-tools-internal/version.properties
index 8f017e2..d394ef2 100644
--- a/build-tools-internal/version.properties
+++ b/build-tools-internal/version.properties
@@ -13,7 +13,7 @@ supercsv          = 2.4.0
 log4j             = 2.26.0
 slf4j             = 2.0.17
 ecsLogging        = 1.7.0
-jna               = 5.12.1
+jna               = 5.13.0
 netty             = 4.1.135.Final
 commons_lang3     = 3.9
 commons_math3     = 3.6.1
@@ -48,7 +48,7 @@ protobuf          = 3.25.5
 # test dependencies
 randomizedrunner  = 2.8.2
 junit             = 4.13.2
-junit5            = 5.12.1
+junit5            = 5.13.0
 hamcrest          = 3.0
 mocksocket        = 1.2
 apache_mina       = 2.2.4
diff --git a/build-tools/build.gradle b/build-tools/build.gradle
index c872d52..872bd35 100644
--- a/build-tools/build.gradle
+++ b/build-tools/build.gradle
@@ -9,6 +9,15 @@
 
 buildscript {
   repositories {
+    maven {
+      url "https://maven.loongnix.cn/loongarch/maven/"
+      content {
+        includeModule "net.java.dev.jna", "jna"
+        includeModule "net.java.dev.jna", "jna-platform"
+        includeModule "org.lz4", "lz4-java"
+        includeModule "com.google.protobuf", "protoc"
+      }
+    }
     mavenCentral()
   }
 }
@@ -121,6 +130,15 @@ configurations {
 }
 
 repositories {
+    maven {
+      url "https://maven.loongnix.cn/loongarch/maven/"
+      content {
+        includeModule "net.java.dev.jna", "jna"
+        includeModule "net.java.dev.jna", "jna-platform"
+        includeModule "org.lz4", "lz4-java"
+        includeModule "com.google.protobuf", "protoc"
+      }
+    }
     mavenCentral()
     gradlePluginPortal()
 }
diff --git a/build-tools/src/main/java/org/elasticsearch/gradle/Architecture.java b/build-tools/src/main/java/org/elasticsearch/gradle/Architecture.java
index 273537d..d6dca28 100644
--- a/build-tools/src/main/java/org/elasticsearch/gradle/Architecture.java
+++ b/build-tools/src/main/java/org/elasticsearch/gradle/Architecture.java
@@ -16,7 +16,8 @@ import java.util.Optional;
 public enum Architecture implements Named {
 
     X64("x86_64", "linux/amd64", "amd64", "x64"),
-    AARCH64("aarch64", "linux/arm64", "arm64", "aarch64");
+    AARCH64("aarch64", "linux/arm64", "arm64", "aarch64"),
+    LOONGARCH64("loongarch64", "linux/loongarch64", "loongarch64", "loongarch64");
 
     public final String classifier;
     public final String dockerPlatform;
@@ -40,6 +41,7 @@ public enum Architecture implements Named {
         return switch (architecture) {
             case "amd64", "x86_64" -> X64;
             case "aarch64" -> AARCH64;
+            case "loongarch64" -> LOONGARCH64;
             default -> throw new IllegalArgumentException("can not determine architecture from [" + architecture + "]");
         };
     }
diff --git a/build.gradle b/build.gradle
index f3948c9..856d64a 100644
--- a/build.gradle
+++ b/build.gradle
@@ -70,6 +70,17 @@ nmcpAggregation {
 
 def nmcpConsumerConfiguration = extensions.getByType(NmcpAggregationExtension).'getConsumerConfiguration$nmcp'()
 allprojects { publishedProject ->
+  repositories {
+    maven {
+      url "https://maven.loongnix.cn/loongarch/maven/"
+      content {
+        includeModule "net.java.dev.jna", "jna"
+        includeModule "net.java.dev.jna", "jna-platform"
+        includeModule "org.lz4", "lz4-java"
+        includeModule "com.google.protobuf", "protoc"
+      }
+    }
+  }
   publishedProject.pluginManager.withPlugin('maven-publish') {
     rootProject.dependencies.add(nmcpConsumerConfiguration.name, rootProject.dependencies.project(path: publishedProject.path))
   }
@@ -304,6 +315,17 @@ subprojects { proj ->
 }
 
 allprojects {
+  repositories {
+    maven {
+      url "https://maven.loongnix.cn/loongarch/maven/"
+      content {
+        includeModule "net.java.dev.jna", "jna"
+        includeModule "net.java.dev.jna", "jna-platform"
+        includeModule "org.lz4", "lz4-java"
+        includeModule "com.google.protobuf", "protoc"
+      }
+    }
+  }
   // We disable this plugin for now till we shaked out the issues we see
   // e.g. see https://github.com/elastic/elasticsearch/issues/72169
   // apply plugin:'elasticsearch.internal-test-rerun'
diff --git a/distribution/archives/build.gradle b/distribution/archives/build.gradle
index ddfdaa6..2875d44 100644
--- a/distribution/archives/build.gradle
+++ b/distribution/archives/build.gradle
@@ -118,6 +118,13 @@ distribution_archives {
     }
   }
 
+  linuxLoongarch64Tar {
+    archiveClassifier = 'linux-loongarch64'
+    content {
+      archiveFiles('tar', 'linux', 'loongarch64', false)
+    }
+  }
+
   linuxTar {
     archiveClassifier = 'linux-x86_64'
     content {
diff --git a/distribution/archives/linux-loongarch64-tar/build.gradle b/distribution/archives/linux-loongarch64-tar/build.gradle
new file mode 100644
index 0000000..4a6dde5
--- /dev/null
+++ b/distribution/archives/linux-loongarch64-tar/build.gradle
@@ -0,0 +1,2 @@
+// This file is intentionally blank. All configuration of the
+// distribution is done in the parent project.
diff --git a/distribution/build.gradle b/distribution/build.gradle
index 7a976ee..96d74ce 100644
--- a/distribution/build.gradle
+++ b/distribution/build.gradle
@@ -353,6 +353,41 @@ configure(subprojects.findAll { ['archives', 'packages'].contains(it.name) }) {
   }
 
   project.ext {
+    localBundledJdkHome = { Project project ->
+      String propertyName = 'loongarchBundledJdkHome'
+      Object propertyValue = project.findProperty(propertyName)
+      if (propertyValue == null) {
+        throw new GradleException("Property [${propertyName}] must point to the JDK to bundle in linux-loongarch64 distributions")
+      }
+
+      File javaHome = project.file(propertyValue.toString())
+      if (javaHome.isDirectory() == false) {
+        throw new GradleException("Bundled linux-loongarch64 JDK home [${javaHome}] does not exist or is not a directory")
+      }
+
+      File releaseFile = new File(javaHome, 'release')
+      if (releaseFile.isFile() == false) {
+        throw new GradleException("Bundled linux-loongarch64 JDK home [${javaHome}] is missing [release]")
+      }
+
+      Properties releaseProperties = new Properties()
+      releaseFile.withInputStream { releaseProperties.load(it) }
+
+      String javaVersion = releaseProperties.getProperty('JAVA_VERSION')?.replace('"', '')
+      if (javaVersion == null) {
+        throw new GradleException("Bundled linux-loongarch64 JDK release file [${releaseFile}] is missing JAVA_VERSION")
+      }
+
+      String expectedMajor = VersionProperties.bundledJdkVersion.split('[+.]')[0]
+      String actualMajor = javaVersion.startsWith('1.') ? javaVersion.split('\\.')[1] : javaVersion.split('\\.')[0]
+      if (actualMajor != expectedMajor) {
+        throw new GradleException(
+          "Bundled linux-loongarch64 JDK at [${javaHome}] has major version [${actualMajor}], expected [${expectedMajor}]"
+        )
+      }
+
+      return javaHome
+    }
 
     /*****************************************************************************
      *                   Common files in all distributions                       *
@@ -430,7 +465,7 @@ configure(subprojects.findAll { ['archives', 'packages'].contains(it.name) }) {
             it.permissions.unix(0644)
           }
         }
-        List excludePlatforms = ['linux-x86_64', 'linux-aarch64', 'windows-x86_64', 'darwin-x86_64', 'darwin-aarch64']
+        List excludePlatforms = ['linux-loongarch64', 'linux-x86_64', 'linux-aarch64', 'windows-x86_64', 'darwin-x86_64', 'darwin-aarch64']
         if (os != null) {
           String platform = os + '-' + architecture
           if (architecture == 'x64') {
@@ -530,7 +565,11 @@ configure(subprojects.findAll { ['archives', 'packages'].contains(it.name) }) {
 
     jdkFiles = { Project project, String os, String architecture ->
       return copySpec {
+        if (os == 'linux' && architecture == 'loongarch64') {
+          from localBundledJdkHome(project)
+        } else {
         from project.jdks."bundled_${os}_${architecture}"
+        }
         exclude "demo/**"
         /*
          * The Contents/MacOS directory interferes with notarization, and is unused by our distribution, so we exclude
diff --git a/gradle.properties b/gradle.properties
index 7c781d8..5fe0a83 100644
--- a/gradle.properties
+++ b/gradle.properties
@@ -17,8 +17,10 @@ org.gradle.java.installations.auto-detect=false
 # log some dependency verification info to console
 org.gradle.dependency.verification.console=verbose
 
-# allow user to specify toolchain via the RUNTIME_JAVA_HOME environment variable
-org.gradle.java.installations.fromEnv=RUNTIME_JAVA_HOME
+# allow user to specify toolchain via the RUNTIME_JAVA_HOME,JDK17_HOME,JDK21_HOME,JDK23_HOME,JDK24_HOME,JDK25_HOME,JDK26_HOME environment variable
+org.gradle.java.installations.fromEnv=RUNTIME_JAVA_HOME,JDK17_HOME,JDK21_HOME,JDK23_HOME,JDK24_HOME,JDK25_HOME,JDK26_HOME
 
 # if configuration cache enabled then enable parallel support too
 org.gradle.configuration-cache.parallel=true
+org.gradle.dependency.verification=off
+org.gradle.java.installations.auto-download=false
diff --git a/gradle/gradle-daemon-jvm.properties b/gradle/gradle-daemon-jvm.properties
index a0a443a..fd67a50 100644
--- a/gradle/gradle-daemon-jvm.properties
+++ b/gradle/gradle-daemon-jvm.properties
@@ -5,5 +5,4 @@ toolchainUrl.MAC_OS.AARCH64=https\://api.adoptium.net/v3/binary/version/jdk-21.0
 toolchainUrl.MAC_OS.X86_64=https\://api.adoptium.net/v3/binary/version/jdk-21.0.7+6/mac/x64/jdk/hotspot/normal/eclipse?project\=jdk
 toolchainUrl.WINDOWS.AARCH64=https\://api.adoptium.net/v3/binary/version/jdk-21.0.7+6/windows/aarch64/jdk/hotspot/normal/eclipse?project\=jdk
 toolchainUrl.WINDOWS.X86_64=https\://api.adoptium.net/v3/binary/version/jdk-21.0.7+6/windows/x64/jdk/hotspot/normal/eclipse?project\=jdk
-toolchainVendor=ADOPTIUM
 toolchainVersion=21
diff --git a/server/src/main/java/org/elasticsearch/bootstrap/BootstrapChecks.java b/server/src/main/java/org/elasticsearch/bootstrap/BootstrapChecks.java
index daaf1dc..ea912c0 100644
--- a/server/src/main/java/org/elasticsearch/bootstrap/BootstrapChecks.java
+++ b/server/src/main/java/org/elasticsearch/bootstrap/BootstrapChecks.java
@@ -585,7 +585,7 @@ final class BootstrapChecks {
 
         @Override
         public BootstrapCheckResult check(BootstrapContext context) {
-            if (isSystemCallFilterInstalled() == false) {
+            if(isSystemCallFilterInstalled() == false && !"loongarch64".equals(System.getProperty("os.arch"))) {
                 final String message = "system call filters failed to install; check the logs and fix your configuration";
                 return BootstrapCheckResult.failure(message);
             } else {
diff --git a/settings.gradle b/settings.gradle
index 4a86e38..53633bf 100644
--- a/settings.gradle
+++ b/settings.gradle
@@ -5,6 +5,15 @@ import org.elasticsearch.gradle.internal.toolchain.EarlyAccessCatalogJdkToolchai
 
 pluginManagement {
   repositories {
+    maven {
+      url "https://maven.loongnix.cn/loongarch/maven/"
+      content {
+        includeModule "net.java.dev.jna", "jna"
+        includeModule "net.java.dev.jna", "jna-platform"
+        includeModule "org.lz4", "lz4-java"
+        includeModule "com.google.protobuf", "protoc"
+      }
+    }
     mavenCentral()
     gradlePluginPortal()
   }
@@ -66,18 +75,8 @@ List projects = [
   'distribution:archives:darwin-tar',
   'distribution:archives:darwin-aarch64-tar',
   'distribution:archives:linux-aarch64-tar',
+  'distribution:archives:linux-loongarch64-tar',
   'distribution:archives:linux-tar',
-  'distribution:docker',
-  'distribution:docker:cloud-ess-docker-export',
-  'distribution:docker:cloud-ess-docker-aarch64-export',
-  'distribution:docker:docker-aarch64-export',
-  'distribution:docker:docker-export',
-  'distribution:docker:ironbank-docker-aarch64-export',
-  'distribution:docker:ironbank-docker-export',
-  'distribution:docker:wolfi-docker-aarch64-export',
-  'distribution:docker:wolfi-docker-export',
-  'distribution:docker:cloud-ess-fips-docker-export',
-  'distribution:docker:cloud-ess-fips-docker-aarch64-export',
   'distribution:packages:aarch64-deb',
   'distribution:packages:deb',
   'distribution:packages:aarch64-rpm',
diff --git a/x-pack/plugin/ml/build.gradle b/x-pack/plugin/ml/build.gradle
index 1e48301..9a313d5 100644
--- a/x-pack/plugin/ml/build.gradle
+++ b/x-pack/plugin/ml/build.gradle
@@ -96,12 +96,8 @@ dependencies {
   api "org.apache.lucene:lucene-analysis-icu:${versions.lucene}"
   api "org.apache.lucene:lucene-analysis-kuromoji:${versions.lucene}"
   implementation 'org.ojalgo:ojalgo:51.2.0'
-  nativeBundle("org.elasticsearch.ml:ml-cpp:${mlCppVersion()}:deps@zip") {
-    changing = true
-  }
-  nativeBundle("org.elasticsearch.ml:ml-cpp:${mlCppVersion()}:nodeps@zip") {
-    changing = true
-  }
+  nativeBundle files("/src/srcs/9.4.3/../ml-cpp-9.4.3-deps.zip")
+  nativeBundle files("/src/srcs/9.4.3/../ml-cpp-9.4.3-nodeps.zip")
   testImplementation 'org.ini4j:ini4j:0.5.2'
   testImplementation "com.google.jimfs:jimfs:${versions.jimfs}"
 }
